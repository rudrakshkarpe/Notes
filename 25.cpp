# include<iostream>

using namespace std;
// logical operators
int main(){

    int age = 23;
    int money = 500;

    if(age>21 && money > 500){
    // if(age<21 || money == 500 || money >=9010 || money >1000){
    cout<<"You are allowed in!" <<endl;
 }
    
    return 0;
}

// && ---> both the cnonditions needs to be true --> to run the code
// || ---> it runs even though when one of the conditions is flase
// || ----> In multiple conditions it'll only look for one of the conditions to be true

    